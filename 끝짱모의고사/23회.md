# 📚 정보처리기사 실기 끝장 모의고사 23회 — 완전 해설 오답노트

> **해설 원칙**: 지문 키워드 → 왜 이게 답인지 → 메커니즘 순서로 서술
> 코드 문제는 **함수 실행을 한 줄씩 따라 내려가며** 변수 변화를 추적 (모든 코드/계산 정답은 실제 실행·검증 완료)

---

## 목차

| 번호  | 유형        | 키워드                          | 난이도 |
| --- | --------- | ---------------------------- | --- |
| Q01 | 용어        | 게이트웨이 / 라우터 / 스위치            | ★★☆ |
| Q02 | C 코드      | 포인터 배열 양끝 교환                 | ★★☆ |
| Q03 | 용어        | AES / LEA                    | ★☆☆ |
| Q04 | Java 코드   | String 메서드 체인               | ★★☆ |
| Q05 | 용어        | 삽입/삭제/갱신 이상                  | ★★☆ |
| Q06 | Python 코드 | dict 정렬 + 조건 필터              | ★★★ |
| Q07 | 용어        | 딥러닝 / LLM                    | ★☆☆ |
| Q08 | SQL       | GROUP BY + HAVING            | ★☆☆ |
| Q09 | Java 코드   | 필드 은닉 + super/캐스팅            | ★★★ |
| Q10 | 용어        | 페이징 / 세그먼테이션 / LRU           | ★★☆ |
| Q11 | C 코드      | 포인터 이동 + static 누적           | ★★★ |
| Q12 | Python 코드 | 클로저 + nonlocal(독립성)          | ★★★ |
| Q13 | 용어        | 화이트/블랙박스/경계값/문장 커버리지         | ★★☆ |
| Q14 | C 코드      | 재귀 거듭제곱(분할)                  | ★★★ |
| Q15 | 용어        | SYN / FIN / RST              | ★★☆ |
| Q16 | SQL       | SUM + NULL 처리                | ★★★ |
| Q17 | 용어        | 쿠키 / 세션                      | ★★☆ |
| Q18 | Java 코드   | try-catch-finally + 참조       | ★★★ |
| Q19 | 관계대수      | σ / π / ⋈                    | ★☆☆ |
| Q20 | 용어        | IaaS / PaaS / SaaS           | ★☆☆ |

---

## Q01. 게이트웨이 / 라우터 / 스위치

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 서로 다른 **프로토콜 변환**              | **게이트웨이** |
| ②  | 네트워크 계층, 최적 경로 선택              | **라우터** |
| ③  | 데이터링크 계층, MAC 학습, 포트 전달        | **스위치** |

### ✅ 정답

① 게이트웨이(Gateway)  ② 라우터(Router)  ③ 스위치(Switch)

---

## Q02. C언어 — 포인터 배열 양끝 교환

### 문제

```c
int a[5] = {2, 7, 3, 8, 1};
int *p = a;
for (i=0; i<2; i++) {          // i=0,1
    temp = *(p+i);
    *(p+i) = *(p+4-i);
    *(p+4-i) = temp;
}
```

### 🤔 이 함수가 하는 일

양끝을 안쪽으로 2번 교환 → 배열 뒤집기(가운데 제외).

### 한 반복씩 따라가기

```
초기: [2, 7, 3, 8, 1]
i=0: a[0]↔a[4] → [1, 7, 3, 8, 2]
i=1: a[1]↔a[3] → [1, 8, 3, 7, 2]
(i=2에서 종료, 가운데 3 유지)
```

### ✅ 정답

```
1 8 3 7 2 
```

---

## Q03. AES / LEA

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답      |
| --- | ------------------------------ | ------- |
| ①  | NIST DES 대체, 128/192/256, 최다 사용 | **AES** |
| ②  | 국내 개발 128비트, 경량 IoT             | **LEA** |

### ✅ 정답

① AES  ② LEA

---

## Q04. Java — String 메서드 체인

### 문제

```java
String s1 = "HelloWorld";
String s2 = s1.substring(3, 7);   // 인덱스 3~6
String s3 = s2.toUpperCase();
String s4 = s3.replace('O', '@');
println(s2); println(s3.charAt(2)); println(s4); println(s1.indexOf("World"));
```

### 한 줄씩 따라가기

```
s1 = "HelloWorld" (H0 e1 l2 l3 o4 W5 o6 r7 l8 d9)
s2 = substring(3,7) = 인덱스 3,4,5,6 = "loWo"
s3 = "loWo".toUpperCase() = "LOWO"
s4 = "LOWO".replace('O','@') = "L@W@"
println(s2) → "loWo"
println(s3.charAt(2)) → "LOWO"[2] = 'W'
println(s4) → "L@W@"
println(s1.indexOf("World")) → "World" 시작 위치 = 5
```

### ✅ 정답

```
loWo
W
L@W@
5
```

---

## Q05. 삽입/삭제/갱신 이상

### 지문 키워드 → 정답

| 번호 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 삽입 시 불필요 데이터 함께 삽입             | **삽입 이상** |
| ②  | 삭제 시 유지할 정보까지 삭제               | **삭제 이상** |
| ③  | 갱신 시 일부만 갱신 → 불일치              | **갱신 이상** |

### ✅ 정답

① 삽입 이상  ② 삭제 이상  ③ 갱신 이상

---

## Q06. Python — dict 정렬 + 조건 필터

### 문제

```python
data = {'a':3, 'b':1, 'c':4, 'd':1, 'e':5}
result = sorted(data.items(), key=lambda x: (x[1], x[0]), reverse=True)
print([k for k, v in result if v % 2 == 1])
print(sum(v for k, v in result if k < 'd'))
```

### 한 단계씩 따라가기

```
items: [('a',3),('b',1),('c',4),('d',1),('e',5)]
정렬 key=(값, 키), reverse=True → 값 내림차순, 같으면 키 내림차순:
   ('e',5), ('c',4), ('a',3), ('d',1), ('b',1)
   (값 1인 d,b는 키 내림차순 → d 먼저)
① 홀수 값(v%2==1)인 키: e(5), a(3), d(1), b(1) → ['e','a','d','b']
② k<'d' 인 값 합: a(3), b(1), c(4) → 3+1+4 = 8
```

### ✅ 정답

```
['e', 'a', 'd', 'b']
8
```
> ⚠️ 정렬은 (값,키) 튜플 내림차순 → 동값 d,b는 키 역순으로 d가 앞. k<'d'는 a,b,c.

---

## Q07. 딥러닝 / LLM

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 인공신경망, 입력/은닉/출력층 다층            | **딥러닝** |
| ②  | 대량 텍스트 사전학습, 문장생성/번역/요약        | **LLM(대규모 언어 모델)** |

### ✅ 정답

① 딥러닝(Deep Learning)  ② 대규모 언어 모델(LLM)

---

## Q08. SQL — GROUP BY + HAVING

### 문제

> 학과별 평균 총점 ≥ 85인 학과와 평균총점 조회

### 단계별 트레이싱

```
학과별 평균:
   컴퓨터: (85+78+90)/3 = 84.33 → <85 제외
   전자: (92+88)/2 = 90 → ≥85 ✓
```

### 정답 SQL

```sql
SELECT 학과, AVG(총점) AS 평균총점
FROM 학생
GROUP BY 학과
HAVING AVG(총점) >= 85;
```
결과: 전자 90

### ✅ 정답 → 위 SQL (결과: 전자 90)

---

## Q09. Java — 필드 은닉 + super/캐스팅

### 문제 (요지)

```java
class A { int x=10; void show(){ print(x); } }
class B extends A { int x=20; void show(){ print(x); print(super.x); } void print2(){ show(); print(x); } }
class C extends B { int x=30; void show(){ print(x); print(super.x); print(((A)this).x); } }
B obj = new C();
print(obj.x);  obj.show();  obj.print();
```
(코드상 B의 메서드명은 print())

### 한 줄씩 따라가기

```
B obj = new C(): obj 선언타입 B, 실제타입 C
obj.x → 필드, 선언타입 B → B.x = 20 → "20 "
obj.show() → 인스턴스 메서드, 실제타입 C → C.show():
   x(C.x)=30 → "30 "
   super.x(B.x)=20 → "20 "
   ((A)this).x → 필드는 캐스팅 타입 A 기준 → A.x=10 → "10 "
obj.print() → B.print(): show() → 실제타입 C → C.show() → "30 20 10 "
   그 후 print(x) → print()는 B에 있으므로 x=B.x=20 → "20 "
```
전체: "20 " + "30 20 10 " + "30 20 10 " + "20 "
= 20 30 20 10 30 20 10 20

### ✅ 정답

```
20 30 20 10 30 20 10 20 
```
> ⚠️ 필드는 참조/캐스팅 타입 기준(obj.x=B.x=20, ((A)this).x=A.x=10). show()는 실제타입 C.

---

## Q10. 페이징 / 세그먼테이션 / LRU

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 고정 크기 블록(페이지/프레임)              | **페이징** |
| ②  | 가변 크기 논리 단위(함수/모듈)             | **세그먼테이션** |
| ③  | 가장 오래 참조 안 된 페이지 교체            | **LRU** |

### ✅ 정답

① 페이징(Paging)  ② 세그먼테이션(Segmentation)  ③ LRU

---

## Q11. C언어 — 포인터 이동 + static 누적

### 문제

```c
int* move(int *p, int n) { return p + n; }
int calc(int *p) { static int s = 1; s += 2; return *p + s; }
int a[] = {5, 10, 15, 20, 25};
int *p = a;  int *q = move(p, 2);   // q = &a[2]
printf("%d ", *p);              // a[0]
printf("%d ", *q);              // a[2]
printf("%d ", calc(p));         // a[0] + s
printf("%d ", calc(q));         // a[2] + s
printf("%d\n", *(move(a, 4)));  // a[4]
```

### 한 줄씩 따라가기

```
p = &a[0], q = move(p,2) = &a[2]
*p = a[0] = 5 → "5"
*q = a[2] = 15 → "15"
calc(p): static s=1 → s+=2 → s=3, return a[0]+3 = 5+3 = 8 → "8"
calc(q): s=3 → s+=2 → s=5, return a[2]+5 = 15+5 = 20 → "20"
*(move(a,4)) = a[4] = 25 → "25"
```

### ✅ 정답

```
5 15 8 20 25
```
> ⚠️ static s는 호출 간 유지(1→3→5). calc(p)=8, calc(q)=20.

---

## Q12. Python — 클로저 + nonlocal(독립성)

### 문제

```python
def outer(x):
    y = x * 2
    def inner(z):
        nonlocal y
        y += z
        return y
    return inner
func1 = outer(3)   # y=6
func2 = outer(5)   # y=10 (별도 클로저)
print(func1(1), func1(2), func2(1), func1(1))
```

### 🤔 핵심 — 각 outer 호출마다 **독립적인 y**(클로저) 생성

### 한 호출씩 따라가기

```
func1 = outer(3): func1의 y = 6
func2 = outer(5): func2의 y = 10 (func1과 독립)

func1(1): y = 6+1 = 7 → 7
func1(2): y = 7+2 = 9 → 9
func2(1): y = 10+1 = 11 → 11  (func2 독립 클로저)
func1(1): y = 9+1 = 10 → 10  (func1의 y는 9에서 이어짐)
```

### ✅ 정답

```
7, 9, 11, 10
```
> ⚠️ func1과 func2는 각자 y를 가진 별도 클로저. func1의 y는 6→7→9→10으로 누적.

---

## Q13. 화이트/블랙박스/경계값/문장 커버리지

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 내부 구조(로직/코드 흐름) 기반             | **화이트박스 테스트** |
| ②  | 외부 기능/명세(입출력) 기반, 내부 무관         | **블랙박스 테스트** |
| ③  | 경계값 중심 케이스 설계                  | **경계값 분석** |
| ④  | 모든 명령문 최소 1회 실행                | **문장 커버리지** |

### ✅ 정답

① 화이트박스 테스트  ② 블랙박스 테스트  ③ 경계값 분석  ④ 문장 커버리지

---

## Q14. C언어 — 재귀 거듭제곱(분할)

### 문제

```c
int func(int n, int m) {
    if (m == 0) return 1;
    if (m == 1) return n;
    if (m % 2 == 0) return func(n*n, m/2);     // 짝수 지수: (n²)^(m/2)
    else return n * func(n, m-1);               // 홀수: n × n^(m-1)
}
// func(2,10), func(3,4)
```

### 🤔 이 함수가 하는 일

**빠른 거듭제곱(분할정복)** — n^m 계산.

### 한 호출씩 따라가기

```
func(2,10): m 짝 → func(4,5)
   func(4,5): m 홀 → 4*func(4,4)
      func(4,4): 짝 → func(16,2)
         func(16,2): 짝 → func(256,1) = 256
      → 256
   → 4*256 = 1024
→ func(2,10) = 1024  (2^10)

func(3,4): 짝 → func(9,2)
   func(9,2): 짝 → func(81,1) = 81
→ 81  (3^4)
```

### ✅ 정답

```
1024
81
```
> 💡 2^10=1024, 3^4=81. 분할정복 거듭제곱.

---

## Q15. SYN / FIN / RST

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답      |
| --- | ------------------------------ | ------- |
| ①  | 3-way 연결 설정 최초 세그먼트            | **SYN** |
| ②  | 4-way 정상 종료 첫 세그먼트            | **FIN** |
| ③  | 비정상 상황 즉시 종료                   | **RST** |

### ✅ 정답

① SYN  ② FIN  ③ RST

---

## Q16. SQL — SUM + NULL 처리

### 문제

```sql
SELECT SUM(C1 + C2 + C3 + C4) AS R1,
       SUM(C1) + SUM(C2) + SUM(C3) + SUM(C4) AS R2
FROM TBL;
```
데이터: (1,2,NULL,4),(3,NULL,5,6),(NULL,2,3,1),(4,5,6,NULL),(7,8,9,1)

### 🤔 핵심 함정 — `C1+C2+C3+C4`에 NULL이 하나라도 있으면 그 **행 전체가 NULL**

### 단계별 트레이싱

**R1 = SUM(C1+C2+C3+C4)** — 행별 합, NULL 포함 행은 NULL
```
행1: 1+2+NULL+4 = NULL (제외)
행2: 3+NULL+5+6 = NULL (제외)
행3: NULL+2+3+1 = NULL (제외)
행4: 4+5+6+NULL = NULL (제외)
행5: 7+8+9+1 = 25  ← 유일하게 NULL 없음
R1 = 25
```

**R2 = SUM(C1)+SUM(C2)+SUM(C3)+SUM(C4)** — 열별 합(NULL 제외)
```
SUM(C1) = 1+3+4+7 = 15  (NULL 제외)
SUM(C2) = 2+2+5+8 = 17
SUM(C3) = 5+3+6+9 = 23
SUM(C4) = 4+6+1+1 = 12
R2 = 15+17+23+12 = 67
```

### ✅ 정답

| R1 | R2 |
| -- | -- |
| 25 | 67 |

> ⚠️ R1은 행 단위 덧셈이라 NULL 있는 행 전체가 NULL(4행 제외) → 25. R2는 열별 SUM(NULL 개별 제외) → 67. 결과가 다름!

---

## Q17. 쿠키 / 세션

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답      |
| --- | ------------------------------ | ------- |
| ①  | 클라이언트 저장, 요청마다 전송, 보안 취약         | **쿠키(Cookie)** |
| ②  | 서버 저장, 식별 정보만 사용, 보안 높음, 자원 사용   | **세션(Session)** |

### ✅ 정답

① 쿠키(Cookie)  ② 세션(Session)

---

## Q18. Java — try-catch-finally + 참조

### 문제 (요지)

```java
static int calc(Box a, Box b, int n) {
    try {
        a.add(n);
        if (n % 2 == 0) throw new RuntimeException();
        b.add(a.value);
        return a.value + b.value;
    } catch (RuntimeException e) {
        b.add(10);
        return b.value;
    } finally {
        a.add(100);
        b.add(1);
    }
}
Box x = new Box(5), y = new Box(3);
int r1 = calc(x, y, 2);   // x,y
int r2 = calc(y, x, 3);   // 순서 바뀜
```

### 🤔 핵심 — finally는 return 전에 실행되지만, return 값은 이미 계산됨. Box는 참조라 변경 누적.

### r1 = calc(x, y, 2) 따라가기 (a=x, b=y)

```
try: a.add(2) → x.value=5+2=7
   n%2==0(2) → RuntimeException 던짐
catch: b.add(10) → y.value=3+10=13
   return b.value → 반환값 계산 = 13 (r1=13 예약)
finally: a.add(100) → x.value=7+100=107
   b.add(1) → y.value=13+1=14
r1 = 13
상태: x=107, y=14
출력: "13,107,14"
```

### r2 = calc(y, x, 3) 따라가기 (a=y, b=x)

```
현재 x=107, y=14
try: a.add(3) → y.value=14+3=17
   n%2==0(3)? 아니오 → 예외 없음
   b.add(a.value) → x.value=107+17=124
   return a.value + b.value = 17 + 124 = 141 (r2=141 예약)
finally: a.add(100) → y.value=17+100=117
   b.add(1) → x.value=124+1=125
r2 = 141
상태: x=125, y=117
출력: "141,125,117"
```

### ✅ 정답

```
13,107,14
141,125,117
```
> ⚠️ return 값은 finally 전에 확정되지만, finally에서 Box(참조) 필드는 계속 변함 → 출력되는 x,y 값은 finally 반영된 값.

---

## Q19. 관계대수 — σ / π / ⋈

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답        |
| --- | ------------------------------ | --------- |
| ①  | 조건 만족 튜플 선택                    | **σ(Selection)** |
| ②  | 특정 속성 추출                       | **π(Projection)** |
| ③  | 공통 속성 기준 결합                    | **⋈(Join)** |

### ✅ 정답

① σ(Selection)  ② π(Projection)  ③ ⋈(Join)

---

## Q20. IaaS / PaaS / SaaS

### 지문 키워드 → 정답

| 빈칸 | 지문 키워드                          | 정답      |
| --- | ------------------------------ | ------- |
| ①  | IT 인프라(서버/스토리지/네트워크) 가상화        | **IaaS** |
| ②  | 개발 플랫폼(OS/개발환경) 제공             | **PaaS** |
| ③  | 완성된 소프트웨어 제공                   | **SaaS** |

### ✅ 정답

① IaaS  ② PaaS  ③ SaaS

---

## 🔑 최종 정답 요약

| 번호 | 정답                                       |
| --- | ---------------------------------------- |
| 01 | ① 게이트웨이 ② 라우터 ③ 스위치                      |
| 02 | 1 8 3 7 2                                |
| 03 | ① AES ② LEA                             |
| 04 | loWo / W / L@W@ / 5                       |
| 05 | ① 삽입 이상 ② 삭제 이상 ③ 갱신 이상                 |
| 06 | ['e','a','d','b'] / 8                     |
| 07 | ① 딥러닝 ② LLM                             |
| 08 | SELECT 학과, AVG(총점) AS 평균총점 ... HAVING AVG(총점)>=85 |
| 09 | 20 30 20 10 30 20 10 20                  |
| 10 | ① 페이징 ② 세그먼테이션 ③ LRU                    |
| 11 | 5 15 8 20 25                             |
| 12 | 7, 9, 11, 10                             |
| 13 | ① 화이트박스 ② 블랙박스 ③ 경계값 분석 ④ 문장 커버리지      |
| 14 | 1024 / 81                                |
| 15 | ① SYN ② FIN ③ RST                       |
| 16 | R1=25 R2=67                             |
| 17 | ① 쿠키 ② 세션                               |
| 18 | 13,107,14 / 141,125,117                  |
| 19 | ① σ ② π ③ ⋈                             |
| 20 | ① IaaS ② PaaS ③ SaaS                     |

---

## 📌 이 회차 핵심 암기 / 자주 틀리는 포인트

1. **Q09 필드 캐스팅**: 필드는 참조/캐스팅 타입 기준. obj.x=B.x=20, super.x=B.x, ((A)this).x=A.x=10. show()만 실제타입 C.
2. **Q11 static 누적**: calc의 static s는 호출 간 유지(1→3→5).
3. **Q12 클로저 독립**: outer 호출마다 별도 y. func1(6→7→9→10), func2(10→11) 독립.
4. **Q14 분할 거듭제곱**: 짝수 지수는 (n²)^(m/2). 2^10=1024, 3^4=81.
5. **⭐ Q16 SUM+NULL**: R1(행별 합)은 NULL 있는 행 전체 NULL→25, R2(열별 SUM)은 개별 제외→67. **결과 다름!**
6. **Q18 finally+참조**: return 값은 finally 전 확정이나, Box(참조) 필드는 finally에서 계속 변경됨.

### 자주 나오는 용어 (이 회차 등장)

- **네트워크 장비**: 게이트웨이(프로토콜 변환)/라우터(IP)/스위치(MAC).
- **암호**: AES/LEA.
- **DB 이상**: 삽입/삭제/갱신 이상.
- **AI**: 딥러닝/LLM.
- **메모리**: 페이징/세그먼테이션/LRU.
- **테스트**: 화이트박스/블랙박스/경계값/문장 커버리지.
- **TCP 플래그**: SYN(연결)/FIN(종료)/RST(강제 종료).
- **웹 상태 유지**: 쿠키(클라이언트)/세션(서버).
- **관계대수**: σ/π/⋈.
- **클라우드**: IaaS/PaaS/SaaS.
